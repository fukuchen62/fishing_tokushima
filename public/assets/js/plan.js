"use strict";

$(window).on('load', function () {
    const $city_id = $('input:hidden[name="city_id"]').val();
    const $level_id = $('input:hidden[name="level_id"]').val();
    const $fish_id = $('input:hidden[name="fish_id"]').val();

    if ($city_id != null) {

    }

    if ($level_id != null) {

    }

    if ($fish_id != null) {

    }

    // if ($page != "") {
    // $('.active1').removeClass('active1');
    //     $('.tab:nth-child(' + $page + ')').addClass('active1');
    // }
});
